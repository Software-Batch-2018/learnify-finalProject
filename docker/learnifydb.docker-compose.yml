version: '3.5'
services:
  postgres:
    container_name: learnify
    image: postgres:latest
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: learnifydbpwd
      POSTGRES_DB: learnify
      PGDATA: /data/postgres
    volumes:
      - learnify:/data/postgres
    ports:
      - '5432:5432'

volumes:
  learnify:
    driver: local
