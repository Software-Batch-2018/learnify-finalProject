import type { Meta } from '@storybook/react';
import { SortDownIcon, SortUpIcon, SortIcon } from './icons';

const Story: Meta<typeof SortDownIcon> = {
  component: SortDownIcon,
  title: 'Icons',
};
export default Story;

export const Primary = {
  args: {},
};
